## Solve x

## Solve x

makeCacheMatrix <- function(x = matrix()) {
        x
}


## Solve x

cacheSolve <- function(x, ...) {
        y <- solve(makeCacheMatrix(x)) ## y is the inverse of x
        y  ## return y
}
